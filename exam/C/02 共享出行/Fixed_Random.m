% 保存当前随机数生成器的状态（"种子"）
scurr = rng();
% 生成随机数
A = randn([1,4])
% A = [-0.4611 0.1132 1.3442 -0.4842];

% 每次想要生成相同的随机数时
% 先用rng函数将生成器的状态置为上次保存的scurr
rng(scurr);
A = randn([1,4])
% A = [-0.4611 0.1132 1.3442 -0.4842];